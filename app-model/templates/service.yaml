apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appmodel.name | default app-migration-frontend}}
  label:
    env: {{ .Values.appmodel.env }}
spec:
  selector:
    app: {{ .Values.appmodel.name }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080